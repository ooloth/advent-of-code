# Advent of Code {year}, Puzzle {day}{part}: https://adventofcode.com/{year}/day/{day}

import sys


def solution(input: str) -> int | None:
    answer = None

    # TODO: investigate if for loops are slow in Python and there's a better default...
    for line in input.splitlines():
        ...

    return answer


def test_solution() -> None:
    examples: list[tuple[str, int]] = [
        ("a", 1),
        ("ab", 2),
        ("abc", 3),
    ]

    for input, expected in examples:
        assert solution(input) == expected


if __name__ == "__main__":
    # Pass the CLI input to the solution and print the result for bin/run to capture
    print(solution(sys.argv[1]))
