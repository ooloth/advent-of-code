# Advent of Code {year}, Day {day}, Part {part}
# https://adventofcode.com/{year}/day/{day}

# TODO: https://github.com/marcelblijleven/adventofcode/blob/master/src/adventofcode/scripts/templates/day_template.txt

from pathlib import Path


def solution(input: str) -> int:
    return len(input)


def test_solution() -> None:
    test_cases: list[tuple[str, int]] = [
        ("a", 1),
        ("ab", 2),
        ("abc", 3),
    ]

    for input, expected in test_cases:
        assert solution(input) == expected


if __name__ == "__main__":
    year_directory = Path(__file__).parent
    puzzle_input_file = f"{year_directory}/inputs/{day}.txt"

    with open(puzzle_input_file) as f:
        puzzle_input_string = f.read().strip()
        print(solution(puzzle_input_string))